{
  "name": "web-socket-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "install-client": "cd client && npm install",
    "build-client": "cd client && npm run build",
    "build": "npm run install-client && npm run build-client",
    "start": "serve -s client/dist"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "mysql2": "^3.14.0",
    "path-to-regexp": "^8.2.0",
    "redis": "^4.7.0",
    "ws": "^8.18.1",
    "serve": "^14.2.4"
  }
}

